server:
  port: 8081

sendgrid:
  api-key: ${SENDGRID_API_KEY:}
  from-email: ${SENDGRID_FROM_EMAIL:no-reply@example.com}
  from-name: ${SENDGRID_FROM_NAME:Notification}

frontend:
  base-url: ${FRONTEND_BASE_URL:http://localhost:3000}
  accept-invite-path: ${FRONTEND_ACCEPT_INVITE_PATH:/accept-invite}
  reset-password-path: ${FRONTEND_RESET_PASSWORD_PATH:/reset-password}
